# USE / SHE / SE Carrier Study

## FUSENGBUWA

Observed substrings include `USE` and `SE`.

- `USE -> ESU` is an exact Roman-character reversal.
- This is **G-REV only** and does not establish a relationship to Esu / Èṣù.

## AGUSHENBI / AGUSENBI / AGUNSEBI

- `SHE` occurs only under particular spellings and is therefore orthographically unstable.
- `SE` survives the widely used form `AGUNSEBI` and also occurs in `FUSENGBUWA`.

## Evidence rule

`SE_MATCH = G_MATCH`, not morpheme identity or etymology.

The documented Fusengbuwa–Ile-Nla–Agunsebi institutional relationship provides an independent **X relationship** between the entities, but `G + X != etymology`.
